{
  "template": "https://example.invalid/copernicus/{cell}.tif",
  "notes": [
    "Replace the template or populate explicit cell URLs.",
    "The batch runner can then auto-download the needed Copernicus geocells on demand."
  ]
}
